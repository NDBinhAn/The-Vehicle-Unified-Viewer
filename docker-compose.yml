version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: vehicle_viewer_redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
    driver: local
